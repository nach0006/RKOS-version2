---
import SolidButton from "./SolidButton.astro";
---

<div class="tilbud">
  <h1>FØRSTE GANG MED KARATE? FÅ FØRSTE <b>MÅNED</b> GRATIS</h1>
  <p>Få en stærk start - træn op til 14 gange!</p>

  <SolidButton variant="black-white" text="KONTAKT" link="/kontakt" page="Kontakt" />
</div>

<style>
  .tilbud {
    display: flex;
    flex-direction: column;
    border-top-right-radius: 70px;
    background-color: var(--red800);
    color: var(--spring-wood50);
    text-align: center;
    justify-content: center;
    align-items: center;
    padding: var(--s);
    padding-left: var(--m);
    margin-right: var(--s);
    margin-top: 65px;
  }

  h1 {
    font-size: 1.6rem;
    font-weight: 400;
    max-width: 22ch;
  }

  p {
    margin: 4px 0 var(--s);
  }

  /** DESKTOP **/
  @media (min-width: 900px) {
    .tilbud {
      border-top-right-radius: 130px;
      padding: 42px 30px 42px 80px;
      margin-right: var(--S);
    }

    h1 {
      font-size: 2.6rem;
      font-weight: 400;
    }
  }
</style>
