---
const { img, description } = Astro.props;
---

<article>
  <img id="hero" src={img} alt=`Backgrounds billede af ${description}` />
  <slot />
</article>

<style>
  article {
    position: relative;
    height: fit-content;
    padding-top: 77.7px; /*da menuen på mobile er fixed, og derfor ikke tager noget af layoutets plads*/
  }

  #hero {
    width: 100%;
    object-fit: cover;
    display: block;
    height: auto;
    max-height: 700px;
  }

  @media (min-width: 900px) {
    article {
      padding-top: 0; /* menuen er nu sticky, derfor er padding-top ikke nødvendig*/
    }
    #hero {
      margin-top: -5%;
    }
  }
</style>
